<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="carpool">
	<resultMap type="carpoolModel" id="carpoolModelRes">
		<result property="no" column="no" />		
		<result property="name" column="name" />		
		<result property="subject" column="subject" />
		<result property="content" column="content" />
		<result property="regdate" column="regdate" />
		<result property="readcount" column="readcount" />
		<result property="slat" column="slat" />		
		<result property="slng" column="slng" />		
		<result property="elat" column="elat" />
		<result property="elng" column="elng" />
		<result property="saddr" column="saddr" />			
		<result property="eaddr" column="eaddr" />
		<result property="startdate" column="startdate" />
		<result property="goal" column="goal" />
		<result property="term" column="term" />
		<result property="charge" column="charge" />
		<result property="smoke" column="smoke" />
		<result property="insure" column="insure" />
		<result property="pnum1" column="pnum1" />
		<result property="pnum2" column="pnum2" />
		<result property="phone" column="phone" />
	</resultMap>
	
	<resultMap type="attendModel" id="attendModelRes">
		<result property="email" column="email" />		
		<result property="name" column="name" />
		<result property="phone" column="phone" />
		<result property="apemail" column="email" />		
		<result property="apname" column="name" />
		<result property="apphone" column="phone" />
		<result property="apsubject" column="subject" />		
		<result property="apcontent" column="content" />
		<result property="no" column="no" />
	</resultMap>	
	
	<!-- 글목록 -->
	<select id="carpoolList" resultMap="carpoolModelRes">
			SELECT * FROM CARPOOL 
			ORDER BY no DESC
	</select>
	
	<insert id="carpoolWrite" parameterType="Integer">
			INSERT INTO CARPOOL
			(NO, SUBJECT, CONTENT, REGDATE, NAME, SLAT, SLNG, ELAT, ELNG, SADDR, EADDR, STARTDATE, GOAL
			 , TERM, CHARGE, SMOKE, INSURE, PNUM1, PNUM2, PHONE )
			VALUES
			(CARPOOL_SEQ.NEXTVAL, #{subject}, #{content}, SYSDATE, #{name}, #{slat}, #{slng}, #{elat}, #{elng}, #{saddr}, #{eaddr}, #{startdate}, #{goal}
			, #{term}, #{charge}, #{smoke}, #{insure}, '0', #{pnum2}, #{phone} )
	</insert>
	
	<select id="carpoolView" resultMap="carpoolModelRes" parameterType="Integer">
			SELECT * FROM CARPOOL 
			WHERE NO = #{no}
	</select>
	
	<!-- 조회수 -->
	<update id="carpoolUpdateReadcount" parameterType="Integer">
			UPDATE CARPOOL
			SET
				READCOUNT = READCOUNT + 1
			WHERE
				NO = #{no}	
	</update>
	<!-- 수정 -->
	<update id="carpoolModify" parameterType="carpoolModel">
			UPDATE CARPOOL
			SET
				<!-- NAME = #{name}, -->
				SUBJECT = #{subject},
				CONTENT = #{content}
			WHERE
				NO = #{no}	
	</update>

	<!-- 글삭제 -->
	<delete id="carpoolDelete" parameterType="Integer">
			DELETE FROM CARPOOL WHERE NO=#{no}
	</delete>

	<!-- 제목검색 -->
	<select id="carpoolSearch0" resultMap="carpoolModelRes" parameterType="String">
		SELECT * FROM CARPOOL
		 WHERE saddr like #{search} or eaddr like #{search}
		 ORDER BY no DESC
	</select>
	
	<!-- 내용검색 -->
	<select id="carpoolSearch1" resultMap="carpoolModelRes" parameterType="String">
		SELECT * FROM CARPOOL
		WHERE content like #{search}
		ORDER BY no DESC
	</select>

	<!-- 작성자검색 -->
	<select id="carpoolSearch2" resultMap="carpoolModelRes" parameterType="String">
		SELECT * FROM CARPOOL
		WHERE name like #{search}
		ORDER BY no DESC
	</select>
	
	<!-- 참가 -->
	<update id="carpoolUpdateAttend" parameterType="Integer">
			UPDATE CARPOOL
			SET
				PNUM1 = PNUM1 + 1
			WHERE
				NO = #{no}	
	</update>
	
	<insert id="attendWrite" parameterType="Integer">
			insert into ATTEND(
			select m.email, m.name, m.phone, t.email, t.name, t.phone, t.subject, t.content, t.no
			from member_c m inner join carpool t on m.name != t.name
			where t.no=#{no} and m.name=#{name}) 
	</insert>
	
	<select id="attendOverlap" resultMap="attendModelRes" parameterType="attendModel">
			<!-- <![CDATA[ 
			SELECT name, no, COUNT(*) as count
			FROM ATTEND
			where name=#{name} and no=#{no}
			GROUP BY name,no
			HAVING COUNT(*) = 1
			]]> -->
	SELECT name, no
			FROM ATTEND
			where name=#{name} and no=#{no}
			
	</select>
	
	<select id="carpoolAttendList" resultMap="attendModelRes">
			SELECT * FROM ATTEND 
			ORDER BY no DESC
	</select>
	
</mapper>